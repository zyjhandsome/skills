# skills

Cursor Agent Skills 集合。

| 技能 | 说明 |
|------|------|
| [content-structuring](./content-structuring/SKILL.md) | 内容结构化整理（v5.22） |

目录结构：

```
content-structuring/
├── SKILL.md              # 技能加载器
└── references/
    └── spec.md           # 完整规范（模板、验收闸门、术语校准等）
```

安装：将整个 `content-structuring/` 目录复制到项目的 `.cursor/skills/` 或 `~/.cursor/skills/` 即可。
