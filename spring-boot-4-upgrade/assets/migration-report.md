# Spring Boot 4 升级记录

填入真实证据；空项写 unknown / unavailable / not-applicable 并说明原因。删除不适用的示例提示，但保留未验证事实。

## 范围与结论

- 模式：assess / migrate
- 状态（只选一个，不新增枚举）：assessed / blocked / implemented-unverified / verified-with-bridges / verified
- 仓库绝对路径、基线 HEAD、当前差异/验证快照：
- 应用/库模块、版本控制位置、profile/configuration：
- 原始声明版本 / 原始解析版本：
- 3.5 中间精确版本 / 最终 4.x 精确版本及选择理由：
- JDK runtime/编译目标、wrapper、部署形态：
- 版本来源 URL、核实日期、支持范围；未知项：
- 验证范围（模块/profile/JDK runtime/数据库/功能）、范围外事项及事先确定的理由：
- 可选结构化证据契约文件与校验结果（记录一致性不等于实际验证）：

## 具体方案与兼容决策

| 阶段 | 模块/预期文件 | 目标版本 | 变化及原因 | 验证命令/环境 | 前置/回退点 |
|---|---|---|---|---|---|

| 组件及实际使用位置 | 源版本/owner | 目标 | 来源/日期 | 官方支持/测试/未知 | 处置与验证 |
|---|---|---|---|---|---|

## 实施与证据

- 持久证据索引/报告位置、可读取性检查、保留期限（适用时）；命令与日志脱敏，密钥仅写注入方式：
- OpenRewrite：插件精确版本、recipe artifact 精确版本、recipe ID 列表；或手工路径及原因。
- discover、dry-run patch 与解析错误/覆盖缺口：
- 实际文件变化与理由（含版本 owner、锁文件、外部配置需求）：
- 原始失败与升级引入失败的区分：
- 3.5 实施检查点（精确解析版本、代码快照、必需检查与证据；失败/缺失时不推进 4.0 实际修改）：
- 逐应用模块/profile 的 core jar 与完整依赖树证据（spring-boot、spring-boot-autoconfigure、旧 Boot 模块反查、制品实际加载）：

| 阶段/检查 | 时间/代码快照 | cwd、完整命令 | JDK/profile/环境 | 退出码 | 测试总数/失败/跳过 | 结果与证据路径 |
|---|---|---|---|---|---|---|

适用验收：解析版本、编译、单元/集成测试、制品、启动或库 consumer、JSON、鉴权负例、数据/消息、监控、native（按需）。不适用项写理由；无环境项保留复跑命令。

- 运行里程碑与最终结果分开写：已初始化部分、实际可用性/请求结果、后续失败及证据；环境归因含对照条件，不将失败记为通过：
- 命中时：HTTP Content-Type/编码变化、预期自动配置与 bean、桥接 serializer 旧数据兼容及集成验证缺口：
- 启动中断后未触达/无法确认的关键 Bean、SDK 或首次调用路径；静态/隔离补测结果及恢复环境后的复跑入口：
- 预编译组件命中时：调用方 jar 坐标/哈希、失效成员与 descriptor、目标 classpath、最终部署制品标识/实际类来源；最终采用的修复及遗留桥，不混用不同轮次结果：
- 单 jar 扩展扫描命中时：有效类总数/成功分析数/缺口，命中类与方法、实际参数类型、路径触达状态及处置；框架类覆盖须另列继承链与消费者验收：
- 配置命中时：生效来源/profile/优先级及未知外部输入、绑定对象/转换器、键和值断言、实际 mapper/bean 行为；离线覆盖和部署未验证范围分开记录：

## 第三方依赖安全

- 组织准入规则/版本；无规则时采用的默认标准；扫描成功、覆盖完整、策略通过分别记录：
- 原始/3.5/候选/最终的工具版本、数据库时间/标识、规则及 suppression；比较条件差异与限制：
- 各模块/profile 的组件清单、原始扫描报告、制品 SHA256/镜像 digest；runtime、test、构建/容器风险和识别盲区：
- 新增/风险上升、原有、已修复、新披露、误报/不适用、待确认分别汇总；不能仅用漏洞总数比较：

| 组件/版本与引入路径 | 公告/别名与来源 | 前后变化/严重度 | 适用条件与证据 | 修复/缓解/例外及依据 | 责任人/有效期 | 复扫与兼容回归证据 |
|---|---|---|---|---|---|---|

扫描不可用及必需覆盖缺失保留未验证；安全例外不等于兼容桥或零风险，发布前复查要求与下一步写入交付。

## 桥接与阻塞

| 类型 | 组件/问题 | 原因和影响 | 负责人/待分配 | 退出或解除条件 | 复查日期 | 验证证据 |
|---|---|---|---|---|---|---|

## 回退与交付

- 代码/制品/配置回退点，用户既有变更的保留方法：
- 新旧服务、消息、缓存/session、schema 兼容情况：
- 临时迁移器/rewrite 配置清理与最终复测：
- 已交付范围、未验证项及发布验收边界：

## 下一步

- 状态及已验证范围：
- 未完成项与解除条件（无则明确写无）：
- 谁／做什么／完成判据；可由 Agent 在当前授权内继续的动作应继续执行：
